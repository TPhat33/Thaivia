"""Thailand Urban Repair map pipeline package.

G0 status: this package provides a working `doctor` command and CLI
scaffolding for `acquire`, `build`, `audit`, `verify`. Those four remain
"not implemented yet (G1)" until the real-map pipeline (G1) is built
against an actually acquired OSM source. See docs/decisions/ for why.
"""

__version__ = "0.1.0"
